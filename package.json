{
  "name": "drift-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @driftdocs/core && npm run build -w @driftdocs/mcp && npm run build -w driftdocs",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
