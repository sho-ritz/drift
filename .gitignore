node_modules/
dist/
*.tsbuildinfo
.DS_Store
# .drift/config.json and .drift/drift.db are committed (dogfooding: the
# links + approval log ARE the state CI checks); only sqlite side files stay out
.drift/*.db-wal
.drift/*.db-shm
obsidian-plugin/main.js
vscode-extension/*.vsix
packages/cli/templates/
packages/cli/README.md
packages/cli/*.tgz
